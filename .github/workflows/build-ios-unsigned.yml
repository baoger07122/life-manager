name: Build Home OS unsigned IPA

on:
  workflow_dispatch:
    inputs:
      render_url:
        description: "The HTTPS Render URL loaded by the native shell"
        required: false
        default: "https://life-manager-1.onrender.com/"

jobs:
  build:
    runs-on: macos-14
    permissions:
      contents: read
    env:
      RENDER_URL: ${{ inputs.render_url || 'https://life-manager-1.onrender.com/' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Capacitor dependencies
        run: npm ci

      - name: Point the local boot shell at the selected Render URL
        run: |
          node <<'NODE'
          const fs = require("fs");
          const path = "www/index.html";
          const renderUrl = process.env.RENDER_URL;
          let source = fs.readFileSync(path, "utf8");
          source = source.replace(/const HOME_OS_URL = "[^"]+";/, `const HOME_OS_URL = "${renderUrl}";`);
          fs.writeFileSync(path, source);
          NODE

      - name: Create and sync the iOS project
        run: |
          npx cap add ios
          npx cap sync ios

      - name: Apply the Home OS app icon
        run: |
          cp native-assets/AppIcon-1024.png ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png

      - name: Build unsigned app bundle
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -target App \
            -configuration Release \
            -sdk iphoneos \
            CONFIGURATION_BUILD_DIR="$PWD/build/Products/Release-iphoneos" \
            build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=""

      - name: Package unsigned IPA
        run: |
          rm -rf Payload HomeOS-unsigned.ipa
          mkdir -p Payload
          cp -R build/Products/Release-iphoneos/App.app Payload/HomeOS.app
          ditto -c -k --sequesterRsrc --keepParent Payload HomeOS-unsigned.ipa

      - name: Upload unsigned IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: home-os-unsigned-ipa
          path: HomeOS-unsigned.ipa
          if-no-files-found: error
